import { Searchfilter7Pipe } from './searchfilter7.pipe';

describe('Searchfilter7Pipe', () => {
  it('create an instance', () => {
    const pipe = new Searchfilter7Pipe();
    expect(pipe).toBeTruthy();
  });
});
